{{#if this.mustShow}}
    <DButton
        @id="chatbot-button"
        @class="btn-floating open-new-message-btn"
        @action={{action this.launchBotchat}}
        @icon={{theme-setting "chatbutton_icon"}}
    />
{{/if}}